Strict Constraints for AI Agents:

DO NOT add any features or conditions that were not explicitly requested.

DO NOT refactor existing business logic unless specifically asked for a refactor.

STRICT ADHERENCE to the provided instructions. If a change is ambiguous, ASK in chat instead of guessing.

PRESERVE all existing comments and formatting.

要求每次進行修改時，必須嚴格遵守以下步驟

步驟1: 列出詳細計劃（用上面鐵律）
步驟2: 等我確認
步驟3: 產生 patch（不直接套用）
步驟4: 等我 review 後執行